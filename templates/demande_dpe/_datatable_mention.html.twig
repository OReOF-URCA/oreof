<div class="space-y-1">
    <p class="font-medium text-secondary-800 dark:text-secondary-100">{{ item.formation.displayLong(false) }}</p>
    <p class="text-xs text-secondary-500 dark:text-secondary-400">{{ item.formation.typeDiplome.libelle|default('Type inconnu') }}</p>
</div>

