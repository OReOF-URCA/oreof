<div class="space-y-1">
    <p class="font-medium text-slate-800 dark:text-slate-100">{{ item.formation.displayLong(false) }}</p>
    <p class="text-xs text-slate-500 dark:text-slate-400">{{ item.formation.typeDiplome.libelle|default('Type inconnu') }}</p>
</div>

