<div class="space-y-1">
    <p class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">
        {{ item.auteur != null ? item.auteur.display : 'Auteur non renseigne' }}
    </p>
    <p class="line-clamp-3 text-sm text-slate-700 dark:text-slate-200">{{ item.argumentaireDemande }}</p>
</div>

