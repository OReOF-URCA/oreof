<div class="space-y-1">
    <p class="text-xs font-semibold uppercase tracking-wide text-secondary-500 dark:text-secondary-400">
        {{ item.auteur != null ? item.auteur.display : 'Auteur non renseigne' }}
    </p>
    <p class="line-clamp-3 text-sm text-secondary-700 dark:text-secondary-200">{{ item.argumentaireDemande }}</p>
</div>

