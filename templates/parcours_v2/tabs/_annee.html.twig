{# todo: deprecated / lien non cliquable #}

<turbo-frame id="tab-content">
    <header class="border-b border-secondary-100 flex justify-between items-center bg-secondary-50/50">
        <div>
            <h2 class="text-xl font-black text-secondary-800 tracking-tight">Année xxx</h2>
            <p class="text-xs text-secondary-400 font-bold uppercase mt-1">Édition des semestres et
                unités d'enseignement</p>
        </div>
        <div class="flex gap-3">
            {#            <button class="px-6 py-2 bg-white border border-secondary-200 rounded-xl text-xs font-black shadow-sm"> #}
            {#                Prévisualiser #}
            {#            </button> #}
            {#            <button class="px-6 py-2 bg-indigo-600 text-white rounded-xl text-xs font-black shadow-lg"> #}
            {#                Enregistrer #}
            {#            </button> #}
        </div>
    </header>

    <div class="flex-1 overflow-y-auto p-12 custom-scrollbar">

    </div>
</turbo-frame>
