{% extends 'base.html.twig' %}

{% set header_description %}
    Créez ou modifiez une question pour une page spécifique.
{% endset %}

{% block header %}{{ faq.id ? 'Modifier' : 'Créer' }} une question FAQ{% endblock %}

{% block content %}
    <div class="w-full">
        <div class="space-y-6">

            <!-- Formulaire dans une card -->
            <div class="rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900 overflow-hidden">
                <div class="p-6">
                    {# Appel du composant Live #}
                    {{ component('faq_editor', { initialFormData: faq }) }}
                </div>
            </div>
        </div>
    </div>

    <script src="/js/editor_helpers.js"></script>
{% endblock %}

{% block breadcrumb %}
    {{ include('communs/_breadcrumb.html.twig', {liens:[
        {
            href:path('app_faq_index'),
            title: 'FAQ',
        },
        {
            href:'',
            title: faq.id ? 'Modifier' : 'Créer',
        }
    ]}) }}
{% endblock %}
