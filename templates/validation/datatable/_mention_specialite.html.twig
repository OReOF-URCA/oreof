{# {% if item.formation.mention != null or item.formation.mentionTexte != null %} #}
{#    {{ item.formation.display }} #}
{# {% else %} #}
{#    {{ component('Badge', { label: 'Non definie', variant: 'danger' }) }} #}
{# {% endif %} #}

