{# {% if item.formation.hasParcours %} #}
{#    <span class="inline-flex flex-wrap items-center gap-2"> #}
{#        <span class="font-medium text-slate-800 dark:text-slate-100">{{ item.parcours.libelle }}</span> #}
{#        {{ item.parcours.typeParcours|badgeTypeParcours }} #}
{#    </span> #}
{# {% else %} #}
{#    <span class="text-slate-400 dark:text-slate-500">Sans parcours</span> #}
{# {% endif %} #}

