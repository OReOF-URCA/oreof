{% extends 'base.html.twig' %}

{% set header_title %}
    {{ help.id ? 'Modifier' : 'Créer' }} une aide
{% endset %}

{% set header_description %}
    Créez ou modifiez une aide contextuelle pour une page spécifique.
{% endset %}

{% block header %}{{ help.id ? 'Modifier' : 'Créer' }} une aide{% endblock %}

{% block content %}
    <div class="w-full">
        <div class="space-y-6">

            <!-- Formulaire dans une card -->
            <div class="rounded-2xl border border-secondary-200 bg-surface shadow-sm dark:border-secondary-700 dark:bg-secondary-900 overflow-hidden">
                <div class="p-6">
                    {# Appel du composant Live #}
                    {{ component('help_editor', { initialFormData: help }) }}
                </div>
            </div>
        </div>
    </div>

    <script src="{{ asset('js/editor_helpers.js') }}"></script>
{% endblock %}

{% block breadcrumb %}
    {{ include('communs/_breadcrumb.html.twig', {liens:[
        {
            href:path('app_help_index'),
            title: 'Gestion des Aides',
        },
        {
            href:'',
            title: help.id ? 'Modifier' : 'Créer',
        }
    ]}) }}
{% endblock %}